# Spatial direction V4

Use fewer, larger compositional moves.

- generous vertical rhythm;
- deliberate asymmetry;
- thin dividers instead of boxed containers where possible;
- avoid nested cards;
- maintain readable line length;
- let photography and type create hierarchy before adding chrome.
