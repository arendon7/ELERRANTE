# Dependency posture

V4 is a visual/editorial redesign on the existing vanilla HTML/CSS/JS stack. Prefer zero or minimal new runtime dependencies. Do not introduce React/Next or a heavy animation library merely to achieve styling or motion that CSS can provide.
