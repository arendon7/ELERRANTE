# Pantry V4

Treat pantry references as authored culinary objects, not a generic product-card wall.

Packshots must use real approved labels/volumes. Generated packaging with approximate typography is reference-only until composited with the correct master artwork.
