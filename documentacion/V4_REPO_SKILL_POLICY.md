# V4 Repo Skill Policy

Project adaptations are intentionally committed rather than relying on a developer's global skill install. This keeps V4 design behavior reproducible for any compatible agent working from the repository.

Upstream sources are re-checked before major releases; updates are reviewed rather than silently replacing local brand-aware adaptations.
