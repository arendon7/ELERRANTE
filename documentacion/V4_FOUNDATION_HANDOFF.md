# V4 Foundation Handoff

The branch now contains enough durable guidance for another design/engineering agent to continue without relying on chat memory. The next agent should begin from `AGENTS.md`, then `el-errante-brand`, then `V4_BRAND_DIRECTION.md`, and only afterward inspect/modify public code.
